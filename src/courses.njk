---
layout: default
title: Time to level up
description: "Courses | gsferreira.com"
---

<section class="my-auto">
  <div class="mx-auto flow">
    <h1>{{ title }}</h1>
    <ul>
      {%- for course in courses.all -%}
        <li class="mt-6">
          <h3 class="mt-1 font-semibold">
            <a class="text-primary hover:text-secondary" href="{{ course.url }}">{{ course.title }}</a>
          </h3>
          <div class="mt-2">
            {% if course.description %}
              {{ course.description }}
            {% endif %}
          </div>
          <div class="mt-2">
            <a class="text-primary hover:text-secondary" href="{{ course.url }}">Visit the Course →</a>
          </div>
        </li>
      {%- endfor -%}
    </ul>
  </div>
</section>