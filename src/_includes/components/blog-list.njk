{% if posts.length > 0 %}
<div class="py-12">
    <div class="mx-auto w-full max-w-6xl px-6 lg:px-16">
        <div>
            <ul class="divide-y divide-charcoal-20 dark:divide-charcoal-80">
                {% for post in posts %}
                    {% if not featured or post.url != featured.url %}
                    <li class="group">
                        <article class="flex flex-col gap-3 py-12 -mx-8 px-8 rounded-card transition-all duration-300 hover:bg-white dark:hover:bg-charcoal-80">
                            <div class="flex items-center gap-3 text-sm">
                                {% if post.data.category %}
                                    <span class="uppercase font-semibold text-charcoal-80 dark:text-charcoal-20">{{ post.data.category }}</span>
                                    <span class="text-charcoal-60 dark:text-charcoal-60 transition-colors" aria-hidden="true">•</span>
                                {% endif %}
                                <time datetime="{{ post.date | htmlDate }}" class="text-charcoal-80 dark:text-charcoal-20">{{ post.date | readableDate }}</time>
                                {% if post.data.readingTime %}
                                    <span class="text-charcoal-80 dark:text-charcoal-20" aria-hidden="true">• {{ post.data.readingTime }} min read</span>
                                {% endif %}
                            </div>
                            <h3 class="text-h3 text-charcoal dark:text-white leading-tight">
                                <a href="{{ post.url }}" class="hover:underline" aria-label="Read post: {{ post.data.title }}">{{ post.data.title }}</a>
                            </h3>
                            {% if post.data.description %}
                                <p class="text-charcoal-80 dark:text-charcoal-20 text-base">{{ post.data.description }}</p>
                            {% endif %}
                        </article>
                    </li>
                    {% endif %}
                {% endfor %}
            </ul>
        </div>
    </div>
</div>
{% endif %} 